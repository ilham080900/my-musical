import { SongTypes } from "@/services/data-types";
import { useState } from "react";

const useLoadSongUrl = (song: any) => {
  // if (!song) {
  //   return;
  // }
  // // const { data: song_name } = song.storage
  // //   .from("songs")
  // //   .getPublicUrl(song.song.id);
  // // return song_name.publicUrl;
};

export default useLoadSongUrl;
